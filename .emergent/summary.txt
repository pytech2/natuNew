<analysis>**original_problem_statement:** The user wants a full-stack web application for a company named **NSTU India Private Limited** to manage property tax notice distribution and surveys. The application requires different user roles (Admin, Surveyor, Supervisor, MC Officer), bulk data upload, property assignment, and a surveyor mobile interface for data collection.

The application has been transformed into a multi-town (multi-tenant) platform where each town has its own separate database, managed by a Super Admin.

**LATEST USER REQUESTS (from the last few messages):**
- Fix the Excel export to include full, clickable Photo URLs, not relative paths.
- Ensure the Excel export is in the correct  format.
- Add Latitude, Longitude, and Time to the submission Excel export.
- Fix the Property ID in the Excel export to show the human-readable ID, not the internal UUID.
- Fix the condition badges in the Submissions table to show Vacant Plot and Wrong Location.
- Add a Property Current Use field to the survey form with options: Residential, Commercial, Mix Use, and Other (with remarks).
- Rename House Locked to Property Locked and House Status to Property Status.
- Implement bulk approval for submissions (up to 100 at a time).
- Redesign the map's survey data pop-up to be more compact.
- Fix Supervisor permissions for approving submissions and viewing assigned colonies/surveyors.

**User's preferred language**: The user communicates in a mix of English and Hindi (e.g., vps update ke liye code de, map ke upar serial number nahi dikha hai ji, excel download me problem). The next agent **MUST** understand and respond in this mixed language format (Hinglish).

**what currently exists?**
A multi-tenant full-stack application with a FastAPI backend and React frontend. The backend uses a monolithic  and a -based middleware to switch between town-specific MongoDB databases. The frontend uses an Axios interceptor to send the current town context with every API request.

The application is feature-rich, with recent additions including bulk submission approval, performance improvements by removing heavy map loads, and extensive survey form enhancements. However, it continues to face a significant, recurring challenge with the user's manual VPS deployment process.

**Last working item**:
- **Last item agent was working:** The agent was fixing the Excel export functionality from the Submissions page. The user reported that photo URLs were relative () instead of absolute (), making them unusable directly from the Excel sheet. The agent updated the backend code to construct the full URL.
- **Status:** IN PROGRESS (Code implemented but not yet verified by the user).
- **Agent Testing Done:** N
- **Which testing method agent to use?** backend testing agent
    - **Test Steps:**
        1. Login as Admin.
        2. Go to the Submissions page.
        3. Export the data to Excel.
        4. Open the downloaded Excel file and verify that the  column contains full, absolute URLs (e.g., ).
- **User Testing Done:** N

**All Pending/In progress Issue list**:
-   **Issue 1:** The user's VPS deployment process is unreliable and a recurring source of problems (P0 - RECURRING BLOCKER).

**Issues Detail:**
-   **Issue 1: User's VPS deployment is failing or out of sync**
    -   **Attempted fixes:** The agent has repeatedly provided a sequence of usage: git [-v | --version] [-h | --help] [-C <path>] [-c <name>=<value>]
           [--exec-path[=<path>]] [--html-path] [--man-path] [--info-path]
           [-p | --paginate | -P | --no-pager] [--no-replace-objects] [--bare]
           [--git-dir=<path>] [--work-tree=<path>] [--namespace=<name>]
           [--super-prefix=<path>] [--config-env=<name>=<envvar>]
           <command> [<args>]

These are common Git commands used in various situations:

start a working area (see also: git help tutorial)
   clone     Clone a repository into a new directory
   init      Create an empty Git repository or reinitialize an existing one

work on the current change (see also: git help everyday)
   add       Add file contents to the index
   mv        Move or rename a file, a directory, or a symlink
   restore   Restore working tree files
   rm        Remove files from the working tree and from the index

examine the history and state (see also: git help revisions)
   bisect    Use binary search to find the commit that introduced a bug
   diff      Show changes between commits, commit and working tree, etc
   grep      Print lines matching a pattern
   log       Show commit logs
   show      Show various types of objects
   status    Show the working tree status

grow, mark and tweak your common history
   branch    List, create, or delete branches
   commit    Record changes to the repository
   merge     Join two or more development histories together
   rebase    Reapply commits on top of another base tip
   reset     Reset current HEAD to the specified state
   switch    Switch branches
   tag       Create, list, delete or verify a tag object signed with GPG

collaborate (see also: git help workflows)
   fetch     Download objects and refs from another repository
   pull      Fetch from and integrate with another repository or a local branch
   push      Update remote refs along with associated objects

'git help -a' and 'git help -g' list available subcommands and some
concept guides. See 'git help <command>' or 'git help <concept>'
to read about a specific subcommand or concept.
See 'git help git' for an overview of the system. and  commands for the user to run. This is a temporary workaround, not a permanent solution.
    -   **Next debug checklist:**
        1.  Instruct the user to run the specific deployment command sequence provided in Critical Info for New Agent.
        2.  If it fails, ask the user for the exact error message or a screenshot from their terminal.
        3.  The most common point of failure is forgetting to run the  command to switch the database name from  to .
    -   **Why fix this issue and what will be achieved with the fix?** The user cannot test or use any new features without a successful deployment. This is the single biggest blocker to project progress.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** Y
    -   **Should Test frontend/backend/both after fix?** N/A (User DevOps)
    -   **Blocked on other issue:** User successfully running the commands on their VPS.

**In progress Task List**:
- None.

**Upcoming and Future Tasks**
**Future Tasks:**
-   Offline support for the surveyor's mobile interface.
-   Add a feature to download all split-employee PDFs as a single ZIP file.
-   Add a shortcut to open the survey form by clicking a map marker.

**Completed work in this session**
- **Excel Export Fixes:**
    - Modified  to include **Latitude, Longitude, and Time**.
    - Corrected the export to use the correct human-readable **Property ID** instead of the internal .
    - **(Last fix)** Updated the endpoint to generate full, absolute **Photo URLs**.
- **Survey Form Enhancements:**
    - Added a new **Property Current Use** field (Residential, Commercial, Mix Use, Other).
    - Added a new special condition button: **Property ID Wrong Location**.
    - Added mandatory **Property Status** field (Kachha, Pakka, Vacant Plot), visible in all survey scenarios.
    - Made the **Remarks** field mandatory when a special condition is selected.
    - Renamed labels: House Locked → **Property Locked** and House Status → **Property Status**.
    - Removed ग्राहक (Customer) from the relation dropdown.
- **Submission Management Improvements:**
    - Implemented **Bulk Approval** for up to 100 submissions at once on the  page.
    - Increased the page limit to show **100 submissions** by default.
    - Fixed condition badges in the submissions table to correctly display **Vacant Plot** and **Wrong Location**.
- **Supervisor Role & Permission Fixes:**
    - Granted Supervisors permission to **approve/reject** submissions.
    - Fixed backend logic to ensure Supervisors can see **assigned colonies and surveyors** on the Properties page.
- **UI/UX & Performance:**
    - **Removed Show Full Town Map** feature to improve application speed, as it was loading up to 50,000 properties at once.
    - Redesigned the **Survey Data dialog** on the  page to be more compact and visually organized.
    - Added an **Upload Old Photos** button with a camera icon to each town card on the  page.
- **Bulk PDF Upload:**
    - Enabled multiple file selection for PDF uploads.
    - Added a limit of **1-5 files** per upload with clear UI hints and validation.

**Earlier issues found/mentioned but not fixed**
- The fundamental VPS deployment workflow remains fragile. A robust CI/CD pipeline or a more stable update script is needed to replace the current error-prone manual commands.

**Known issue recurrence from previous fork**
- **Issue recurrence in previous fork:** User's VPS deployment is failing or out of sync.
- **Recurrence count:** Very High (present in almost every fork).
- **Status:** IN PROGRESS

**Code Architecture**


**Key Technical Concepts**
-   **Multi-Tenancy with ContextVar:** Using Python's  for request-scoped DB connections.
-   **Axios Interceptors:** Frontend interceptor adds  header to all API calls.
-   **Dynamic Database Connections:**  function connects to the correct MongoDB instance.
-   **Bulk Actions (React):** Using state () to track selected items and perform batch API calls.
-   **Excel Generation (Backend):** Using  and  to create and serve  files from API endpoints.

**key DB schema**
-   ** ()**
    -   ****: 
    -   ****: 
-   ** (e.g., )**
    -   ****: 
    -   ****: 
    -   ****, , .

**All files of reference**
-    (Main backend logic, heavily modified for exports, permissions, and new survey fields)
-    (Major changes for bulk approve, UI fixes, and new data display)
-    (Major changes for new fields, labels, and conditional logic)
-    (Dialog redesign, feature removal)
-    (Added upload photo shortcut)
-    (PDF upload limit implementation)

**Areas that need refactoring**:
-   The monolithic  is extremely long and difficult to maintain. It urgently needs to be broken down into smaller modules using FastAPI's .
-   The VPS deployment process is the most critical issue. A stable, repeatable deployment script is desperately needed.

**key api endpoints**
-   : **Heavily Modified**. Added latitude, longitude, time, correct property ID, and full photo URLs.
-   : **New**. Approves a list of submission IDs.
-   : **Modified**. Now accepts , , , and .
-   , : **Modified**. Permissions updated to allow Supervisors access.
-   : **Modified**. Permissions updated to allow Supervisors.

**Critical Info for New Agent**
-   **Prioritize Verification:** Your first task is to guide the user to test the latest fix: the **full photo URLs in the Excel export**.
-   **VPS DEPLOYMENT IS THE BIGGEST HURDLE:** The user will likely report that recent changes are not live. Be prepared to provide the exact deployment commands below. The  command is absolutely critical.
    
up to date, audited 1566 packages in 5s

284 packages are looking for funding
  run `npm fund` for details

22 vulnerabilities (4 low, 6 moderate, 12 high)

To address issues that do not require attention, run:
  npm audit fix

To address all issues possible (including breaking changes), run:
  npm audit fix --force

Some issues need review, and may require choosing
a different dependency.

Run `npm audit` for details.

> frontend@0.1.0 build
> craco build

Creating an optimized production build...
Failed to compile.

static/css/main.bf7ced44.css from Css Minimizer plugin
Error: Cannot find module '/app/frontend/node_modules/postcss-value-parser/lib/index.js'. Please verify that the package.json has a valid "main" entry
-   **User Communication:** Communicate in Hinglish. The user is providing direct, actionable feedback.
-   **Monolithic Backend:**  is huge. Use search () to find endpoints and logic before editing.

**documents and test reports created in this job**
-    (updated)
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **Pending:** User specified export me only, clarifying the issue is with the Excel export, not a PDF download problem. Agent understood this to mean the photo URLs need to be fixed.
2.  **Completed:** User sent a screenshot showing the exported Excel file and complained it downloads in a strange format (likely meaning the data inside, like URLs, is not usable).
3.  **Completed:** User reported that Longitude, Latitude, and Time are missing from the Excel export and that Photo URLs should be clickable.
4.  **Completed:** User reported the Property ID in the exported Excel was the internal UUID, not the correct one.
5.  **Completed:** User reported that Vacant Plot and Wrong Location badges were not showing in the Submissions table's CONDITION column.
6.  **Completed:** User requested to add a new field Property Current Use (Residential, Commercial, etc.) and rename House Status to Property Status.
7.  **Completed:** User requested several changes to the submission view: show 100 items, allow bulk approval, and ensure all survey data (like location) is visible in the details.
8.  **Completed:** User requested to redesign the survey data pop-up on the map to be more compact.
9.  **Completed:** User reported that Supervisors could not approve submissions and could not see colonies/surveyors to assign.
10. **Completed:** User requested that the House Status field should be visible even when a special condition (like Property Locked) is selected.

**Project Health Check:**
-   **Broken:**
    -   The user's VPS deployment workflow is highly unreliable and a constant source of friction.
-   **Mocked:** None

**3rd Party Integrations**
-   MapLibre GL & React-Map-GL
-   PyMuPDF, reportlab
-   pandas, openpyxl
-   wkhtmltopdf

**Testing status**
-   **Testing agent used after significant changes:** YES, for the initial batch of fixes.
-   **Troubleshoot agent used after agent stuck in loop:** NO
-   **Test files created:** None
-   **Known regressions:** None.

**Credentials to test flow:**
-   **Admin:**  / 
-   **Surveyor:**  / 
-   **Supervisor:**  / 
-   **MC Officer:**  / 

**What agent forgot to execute**
- The agent successfully implemented all the user's requests from this session. The main outstanding item is not a forgotten task but the strategic problem of the fragile VPS deployment process, which needs a more robust solution than providing manual commands.</analysis>
