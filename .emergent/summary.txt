<analysis>**original_problem_statement:** The user wants a full-stack web application for a company named **NSTU India Private Limited** to manage property tax notice distribution and surveys. The application requires different user roles (Admin, Surveyor, Supervisor, MC Officer), bulk data upload, property assignment, and a surveyor mobile interface for data collection.

The latest major requirement is to transform the application into a multi-town (multi-tenant) platform. Each town should have its own separate data (properties, bills, employees), while a Super Admin can manage all towns. The login flow must now include a town selection step. The town of Thanesar should retain all existing data, which is stored in the  database.

**PRODUCT REQUIREMENTS:**
- **Multi-Tenancy:**
    - The app must support multiple towns with isolated data.
    - Admins log in and select a town. All subsequent data operations are scoped to that town.
    - Super Admins can manage and switch between all towns. Other users are assigned to a single town.
- **Data & Workflow:**
    - Manage towns (create/edit).
    - Manage town-specific users.
    - Upload data (PDF/Excel) for a specific town.
    - Assign properties and colonies to surveyors.
    - Track progress via a town-specific dashboard with All Time and Today views.
    - Surveyor mobile interface for data collection.
- **Exports & Reports:**
    - Advanced search on submissions with export to Excel.
    - PDF bill generation with flexible filtering (e.g., skipping vacant plots or 'NA' owner names).
    - Excel exports should have clickable photo URLs.
- **New User Requests (from last message):**
    - A long list of new bugs and features including: old photo migration from an Excel file, block assignment of colonies, fixing Excel export formats, and improving the property map view.

**User's preferred language**: The user communicates in a mix of English and Hindi (e.g., vps par old print aa rha, photo update kar do ji). The next agent **MUST** understand and respond in this mixed language format.

**what currently exists?**
A full-stack application (FastAPI backend, React frontend) with a robust multi-tenant architecture. The system now uses a  for global entities (users, towns) and separate databases for each town's data.

The core multi-tenancy implementation is complete:
- **Backend:** A  middleware automatically sets the correct town database based on an  request header. All data-access functions now use  to read from the correct town-specific database.
- **Frontend:** An  interceptor automatically attaches the  header to every API call, ensuring all UI actions are correctly scoped to the selected town.

Recent features include a dashboard Today Report toggle, flexible filtering for PDF generation, and town-scoped image URLs. However, the user continues to face significant challenges deploying these updates to their VPS, leading to a disconnect between the developed features and what they see live.

**Last working item**:
- **Last item agent was working:** The agent received a long list of new feature requests and bug reports from the user (message #558) and was in the process of creating a prioritized plan to address them. The plan was formulated but not yet presented to the user for confirmation.
- **Status:** NOT STARTED
- **Agent Testing Done:** N
- **Which testing method agent to use?** both
- **User Testing Done:** N

**All Pending/In progress Issue list**:
- **Issue 1:** New list of bugs and features requested by the user (P0 - BLOCKER)
- **Issue 2:** User's VPS deployment is failing or out of sync (P0 - RECURRING BLOCKER)
- **Issue 3:** Excel export is generating a file in the wrong format (P1)
- **Issue 4:** Excel export is missing the  column (P1)
- **Issue 5:** Surveyor's mobile number is not showing in the Block Un-assign UI (P2)

**Issues Detail:**
- **Issue 1: New list of bugs and features requested by the user**
    - **Attempted fixes:** The agent has created a plan to tackle these but has not started implementation.
    - **Next debug checklist:** The new agent must present the plan to the user for approval and then begin implementation, starting with the highest priority items from the user's list (see **Upcoming Tasks**).
    - **Why fix this issue and what will be achieved with the fix?** To address the user's latest and most urgent requirements.
    - **Status:** NOT STARTED
    - **Is recurring issue?** N
    - **Should Test frontend/backend/both after fix?** Both
    - **Blocked on other issue:** None.

- **Issue 2: User's VPS deployment is failing or out of sync**
    - **Attempted fixes:** The previous agent provided numerous manual patch scripts (, , python scripts to rewrite ) because  kept failing due to local changes on the VPS. This is a fragile and error-prone process.
    - **Next debug checklist:**
        1.  Instruct the user to first commit their local changes on the VPS or stash them before pulling. A better command sequence would be No local changes to save.
        2.  Provide a single, reliable script for rebuilding and restarting services ( for backend, , and  for frontend).
        3.  The  fix () needs to be applied reliably after each pull.
    - **Why fix this issue and what will be achieved with the fix?** The user is unable to see or test any of the new features. Resolving this is critical for project progress.
    - **Status:** IN PROGRESS
    - **Is recurring issue?** Y
    - **Should Test frontend/backend/both after fix?** N/A (User DevOps)
    - **Blocked on other issue:** User successfully running the commands on their VPS.

**In progress Task List**:
- None.

**Upcoming and Future Tasks**
**Upcoming Tasks (from user's latest request):**
- **(P0) Old Photo Migration:** Create a script to read . For each row, find the property by ID in the Ladwa (LDA) town database and update its .
- **(P0) Fix Excel Export:**
    - The export from PDF Bills should download a valid  file.
    - The export must include the  column.
- **(P1) Block Assign Colonies:**
    - Create a UI similar to Block Assign Surveyors.
    - Allow an admin to select multiple colonies and assign them to multiple surveyors.
    - Also implement Block Un-assign Colonies.
- **(P1) Add to Property Duplicate Prevention:** When running Add to Property from a PDF, check if a property ID already exists in the database. If so, show a message or status indicating it was skipped to prevent duplicates.
- **(P2) Town-wide Property Map:** On the Property Map page, add a button/option Show Full Town Map. When clicked, it should display markers for all properties in the selected town.
- **(P2) Simple Text Change:** In the Relation dropdown, change पड़ोसी ग्राहकदार to ग्राहक.
- **(P2) Bulk PDF Upload:** Modify the PDF upload feature to allow selecting and processing files for multiple colonies at once.

**Future Tasks:**
- Offline support for the surveyor's mobile interface.
- Add a feature to download all split-employee PDFs as a single ZIP file.
- Add a shortcut to open the survey form by clicking a map marker.

**Completed work in this session**
- **Full Multi-Tenant Scoping:** Implemented backend middleware () and a frontend  interceptor to make all data operations town-aware, completely isolating town data.
- **User Scoping:** Ensured new towns start with 0 employees and new users are correctly assigned to the selected town.
- **Dashboard Today Report:** Added a toggle, date picker, and CSV download for daily reports.
- **Dashboard Pie Chart Fix:** Corrected the dashboard pie chart to use the right data keys (, ).
- **Town-Scoped Image URLs:** Modified image URLs to include the town code (e.g., ) and added a backend fallback to find old images without a town code, fixing broken images in new towns.
- **PDF Generation Filtering:** Replaced the single dropdown with two separate checkboxes (Skip NA Names, Skip Vacant Plots) for more granular filtering when generating PDFs and adding properties from bills.
- **PDF Upload Logic:** Removed filtering during PDF upload. The system now uploads all data, and filtering is handled at the time of PDF generation.
- **Extensive VPS Debugging:** Provided numerous direct-patch scripts to the user to fix their out-of-sync VPS environment.

**Code Architecture**


**Key Technical Concepts**
- **Multi-Tenancy with ContextVar:** Using Python's  in a FastAPI middleware to manage request-scoped database connections, providing a clean way to ensure data isolation.
- **Axios Interceptors:** Using interceptors in the frontend to automatically modify all outgoing API requests to include the necessary  header.
- **Dynamic Database Connections:** Using  to dynamically connect to the correct MongoDB database based on the selected town.
- **Remote Server Patching:** Using Python scripts executed via SSH to directly modify files on the user's VPS as a workaround for usage: git [-v | --version] [-h | --help] [-C <path>] [-c <name>=<value>]
           [--exec-path[=<path>]] [--html-path] [--man-path] [--info-path]
           [-p | --paginate | -P | --no-pager] [--no-replace-objects] [--bare]
           [--git-dir=<path>] [--work-tree=<path>] [--namespace=<name>]
           [--super-prefix=<path>] [--config-env=<name>=<envvar>]
           <command> [<args>]

These are common Git commands used in various situations:

start a working area (see also: git help tutorial)
   clone     Clone a repository into a new directory
   init      Create an empty Git repository or reinitialize an existing one

work on the current change (see also: git help everyday)
   add       Add file contents to the index
   mv        Move or rename a file, a directory, or a symlink
   restore   Restore working tree files
   rm        Remove files from the working tree and from the index

examine the history and state (see also: git help revisions)
   bisect    Use binary search to find the commit that introduced a bug
   diff      Show changes between commits, commit and working tree, etc
   grep      Print lines matching a pattern
   log       Show commit logs
   show      Show various types of objects
   status    Show the working tree status

grow, mark and tweak your common history
   branch    List, create, or delete branches
   commit    Record changes to the repository
   merge     Join two or more development histories together
   rebase    Reapply commits on top of another base tip
   reset     Reset current HEAD to the specified state
   switch    Switch branches
   tag       Create, list, delete or verify a tag object signed with GPG

collaborate (see also: git help workflows)
   fetch     Download objects and refs from another repository
   pull      Fetch from and integrate with another repository or a local branch
   push      Update remote refs along with associated objects

'git help -a' and 'git help -g' list available subcommands and some
concept guides. See 'git help <command>' or 'git help <concept>'
to read about a specific subcommand or concept.
See 'git help git' for an overview of the system. issues.

**key DB schema**
- ** ()**
  - ****: 
  - ****:  (Global user table).
- ** (e.g.,  for Thanesar)**
  - ****, ****, ****, , .

**All files of reference**
-  (Main backend logic)
-  (Axios interceptor)
- 
- 
- 
- 
-  (Artifact for photo migration)

**Areas that need refactoring**:
- The monolithic  file is over 3000 lines long and getting very difficult to manage. It should be broken down into logical modules using FastAPI's  (e.g., , , ).
- The VPS deployment process is broken and relies on manual, fragile patching. A stable, repeatable deployment workflow is desperately needed.

**key api endpoints**
- : Updated with  and  flags.
- : Updated with  and  flags.
- : Now correctly filters users by the selected town via  field.
- : Now automatically assigns the  based on the selected town.
- : New route to serve town-specific files.
- : Legacy route, now with a fallback to search across all town databases.

**Critical Info for New Agent**
- **Prioritize User's New List:** The user has provided a long list of urgent tasks in their last message. You must start by presenting a plan to tackle these, with photo migration and Excel export fixes being top priority.
- **VPS DEPLOYMENT IS THE BIGGEST HURDLE:** The user's VPS is constantly out of sync. Be prepared for extensive, patient, step-by-step guidance. Standard  often fails for them. You will likely need to provide robust commands that handle local changes (No local changes to save) and then reliably apply environment-specific fixes (like the ).
- **Production DB Name:** The user's original and most important town, Thanesar, uses the database named . This is hardcoded in a mapping in  and is a critical piece of information.
- **Use the Plan:** The previous agent already created a detailed plan in the last  call (message #560). Use this as a starting point for your own plan.

**documents and test reports created in this job**
- 
-  (updated)

**Last 10 User Messages and any pending HUMAN messages**
1.  **PENDING:** User provided a long list of new features and bug fixes, including photo migration, block colony assignment, fixing Excel exports, and more.
2.  **Completed:** User asked about the Add to Property skip functionality, which the agent implemented.
3.  **Completed:** User asked for individual checkboxes (Skip NA, Skip Vacant) for PDF generation instead of a dropdown, which the agent implemented.
4.  **Completed:** User reported  failing on VPS. The agent provided a fix to discard local changes and pull again.
5.  **Completed:** User reported that no-name data was still being skipped during PDF *upload*. The agent fixed this to ensure all data is uploaded and filtering only happens at print time.
6.  **Completed:** User reported that even after fixes, colony assignment wasn't working for a new town on their VPS. The agent diagnosed it as a stale frontend build and provided the command to rebuild.
7.  **Completed:** User reported new employees were not showing up in a new town. The agent fixed the user listing/creation endpoints to be correctly town-scoped.
8.  **Completed:** User reported dashboard data was incorrect for new towns. The agent fixed a bug in the pie chart logic.
9.  **Completed:** User reported images were not showing for new towns on VPS. The agent implemented town-scoped image URLs and a fallback for old URLs.
10. **Completed:** User reported login issues and data loss on VPS. The agent diagnosed that the wrong database was being used ( instead of ) and provided multiple patch scripts to fix it.

**Project Health Check:**
- **Broken:**
    - The user's VPS deployment workflow is highly unreliable and a constant source of issues.
    - Excel export functionality is not working as expected (wrong format, missing columns).
- **Mocked:** None

**3rd Party Integrations**
- MapLibre GL & React-Map-GL
- PyMuPDF, reportlab
- pandas, openpyxl
- wkhtmltopdf (via an external command)

**Testing status**
- **Testing agent used after significant changes:** YES, but many changes were made after the last run.
- **Troubleshoot agent used after agent stuck in loop:** NO
- **Test files created:** None
- **Known regressions:**
    - Excel export functionality seems to have regressed.

**Credentials to test flow:**
- **Admin:**  / 
- **Surveyor:**  / 
- **Supervisor:**  / 
- **MC Officer:**  / 

**What agent forgot to execute**
- The agent was in the middle of planning how to address the user's latest large set of requests. The execution of that plan is pending for the next agent.</analysis>
